{
  "name": "dots2",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "build:dev": "bun build src/index.ts --outfile .build/main.js --target=browser --sourcemap=inline --watch"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "remeda": "^2.11.0"
  }
}
